html
  head
    link(rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous")
    title PUG

body
  div(class="jumbotron container mt-3")

    h3 Productos
        if products.length
        div
            table(class="table table-dark")
                tr(class="text-warning")
                    th Titulo
                    th Precio
                    th Imagen
                
                each item in products  
                    tr
                    td=item.title
                    td=item.price
                    td
                      img(style="width: 100px;" src=item.thumbnail alt=item.title)

<!-- unless dice : ejecuta esto a menos que esta condicion sea verdadera, es decir, si products esta vacio no envia un mensaje informandonos que no hay datos-->
    unless products.length
      h3(class="alert alert-danger") No se encuentran datos.

    a(class="btn btn-primary" href="/") Form
